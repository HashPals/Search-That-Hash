[tool.poetry]
name = "search-that-hash"
version = "0.1.0"
description = "Search hashes quickly before cracking them"
authors = ["Jayy", "bee"]

[tool.poetry.dependencies]
python = "^3.7"
click = "^7.1.2"
appdirs = "^1.4.4"
toml = "^0.10.1"
loguru = "^0.5.2"
name-that-hash = "^0.8.0"
requests = "^2.25.1"
rich = "^9.10.0"

[tool.poetry.dev-dependencies]
pytest = "^6.2.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
sth = "search_that_hash.main:main"
name-that-hash = "search_that_hash.main:main"
